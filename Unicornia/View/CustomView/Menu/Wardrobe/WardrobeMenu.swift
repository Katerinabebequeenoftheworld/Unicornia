import SwiftUI

struct WardrobeMenu: View {

    var body: some View {
        Rectangle()
        .fill(Color.init(hexString: "C800AA"))
            .opacity(0.5)
            .cornerRadius(20)
            .frame(height: 80)
    }
}
